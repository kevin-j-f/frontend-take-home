import React from 'react';

const Error = () => (
	<div className={'package-list__error'}>
		<span className={'package-list__error__icon'}>!</span>
		Error loading packages
	</div>
);

export default Error;